import { FormControl } from "react-bootstrap"

const ESInput = () => {
    return (
        <FormControl
            placeholder="Username"
            aria-label="Username"
            aria-describedby="basic-addon1"
            onClick={() => {}}
        />
    )
}

export { ESInput }